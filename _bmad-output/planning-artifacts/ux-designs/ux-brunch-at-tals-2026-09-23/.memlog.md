---
topic: בראנץ׳ אצל טל — UX
updated: 2026-09-23T20:11
---

- (event) import: imports/refrence-desing.md (Ziona Cafe DESIGN.md) — user-supplied visual reference
- (decision) Sources confirmed: SPEC.md, site-map.md, products-catalog.md, notification-matrix.md, cancellation-rules.md
- (override) Palette: 'almost like the reference' (Ziona) — restrained cream/ink/sand-olive; colorfulness comes mainly from food photography. Overrides site-map 'צבעוני' wording by user choice.
- (decision) Light mode only; no dark mode
- (decision) Display type: handwritten-like Hebrew like the reference; Ziona fonts are private Wix fonts so pick Google Fonts Hebrew equivalents
- (decision) UI system: shadcn/ui (base-nova, rtl:true, lucide icons) — spines specify deltas only
- (decision) Form factor: mobile-first responsive web + PWA; three surfaces: public site, customer area, admin panel (phone-first too)
- (assumption) Stakes: consumer with personal data (babies, allergies) and money; accessibility floor WCAG 2.1 AA / Israeli standard 5568
- (decision) Working mode: coaching path with visual HTML options
- (decision) Protagonists (proposed, user accepted, may revise): Noa (32, baby girl 3 months, discovers Tal on Instagram — guest); Michal (new customer, paid via Bit for a punch card, gets activation link); Dana (returning, must cancel because baby is sick); Tal (approves payment on phone between batches of dough)
- (decision) Customer climax beat: 'המקום שלך סביב השולחן שמור' — booking confirmation showing date, what was used, cancel-by deadline
- (decision) Tal admin usage: phone between tasks (quick actions), desktop for long work (content, import, export, audit log), and session-morning quick view (attendees, babies, allergies)
- (event) Rendered .working/color-themes-1.html (4 variants) and .working/typography-1.html (4 pairings)
- (decision) Palette chosen: variant 2 'קרם וזית' — bg #FAF6EE, card #FFFDF8, ink #2E2A1F, ink-muted #6B6450, border #E6DFCF, accent #8A875A (decorative only; ink on accent 3.88:1 fails — no body text on accent), primary #4A4A2A / on-primary #FAF6EE, success #4E6B34, warning #8C5E14, error #B42318, pending #4A5A6A, expired #676154
- (event) User rejected all 4 Google Fonts pairings: 'not similar to the reference at all'. Need actual Ziona font identity.
- (change) Identified actual Ziona fonts from zionacafe.co.il CSS: display = 'Ezer Block Pro Light' (Oded Ezer, commercial; wfont_* ids map to orig_oe_ezer_block_pro_light), body = Almoni (AlefAlefAlef, commercial). Reference's 'handwritten-like' description was wrong; supersedes the handwritten display decision.
- (decision) Customer area nav: bottom tab bar — בית · מפגשים · ההרשמות שלי · התראות · פרופיל (balances/cards reachable from home + profile)
- (decision) Admin nav (phone): bottom tabs בית · מפגשים · לקוחות · תשלומים · עוד (products, content, broadcast, import, audit log, settings); on desktop becomes side nav
- (decision) Admin home leads with the next session card (occupancy, babies, allergies), then 'action needed' list, then refunds/payments/expiring cards/sum
- (decision) Voice: warm and personal like Tal speaking — feminine second person, food/table imagery, calm errors that always offer a way forward
- (decision) Typography chosen (typography-2 pairing 1): display = Heebo 200/300 (Hebrew by Oded Ezer — closest free kin to Ezer Block Pro Light; weight 200 only ≥28px); body = Assistant 400 (closest to Almoni), 16–17px; numbers/dates always in body font
- (decision) Punch-card multi-booking: multi-select up to remaining entries (choosing fewer than 4 is fine), one summary + one confirm; each booking validated separately server-side, partial success shows which were saved and which failed with reason
- (decision) Booking confirmation opens as a bottom sheet over the session page (what will be used, validity, last self-cancel time), confirm button in thumb reach
- (decision) Motion: minimal — short soft transitions only, respect prefers-reduced-motion
- (event) Rendered .working/directions-1.html — 3 home directions: שקט של Ziona / השולחן במרכז / תפריט ביתי
- (decision) Home direction: direction 1 'שקט של Ziona' (airy, type-led, thin-divider session list, round WhatsApp FAB in corner) combined with direction 2's hero (full-bleed top-down table photo with wordmark, headline and CTA over it)
- (decision) Add public page 'הצהרת נגישות' (footer link), content edited by Tal via site content (draft/preview/publish like privacy policy). No overlay accessibility widget — accessibility is built in. Needs back-propagation to SPEC site-map.
- (event) IA closure check done: all site-map surfaces covered by journeys; implied SPEC surfaces added (login, activation/onboarding form + link states, PWA install guide, offline screen, push opt-in explainer, blocked-action states)
- (event) Discovery complete; starting Finalize distillation
- (change) Join CTA also from direction 2: full-width sticky bottom WhatsApp bar 'להצטרפות צרי קשר עם טל' on the public site, replacing direction 1's round corner FAB
- (decision) Public nav: menu button in top bar opens Sheet with page list
- (decision) Thresholds: few-left chip at ≤3 places; entitlement 'about to expire' at ≤10 days
- (decision) Push explainer offered right after first successful booking (at the 'המקום שלך שמור' moment)
- (decision) Admin session-morning view: online only, no offline caching of attendee personal data (consistent with CAP-23)
- (change) Review fixes (a11y B1/B2): hero scrim now >=72% ink behind the whole text block and a 72% band behind the top bar (on-primary >=5.35:1 even over white); hero wordmark display-xl weight 300 (200 only >=28px on cream); hero min-height 560px; over the hero the menu + login buttons become solid background/ink chips. Typographic fallback stays default until a photo passes the rule.
- (change) Review fixes (a11y M1): focus ring = new component focus-ring — two-tone :focus-visible, 2px on-primary offset band + 2px primary outer ring (Tailwind ring-2 ring-primary ring-offset-2 ring-offset-background); works on bg/card/muted (7.7-9:1), primary 8.46, whatsapp-bar 5.6, destructive 6.1, inputs (ring outside border), hero scrim >=5.35. --ring assumption closed.
- (change) Review fixes (a11y M2-M8): visible X close in bottom-sheet/menu-sheet, dialogs focus their title on open (never the confirm/destructive button); explicit focus destination after booking/partial result/cancel/sensitive confirm; single-target rows (session-row link with ::after, 'לפרטים' aria-hidden, select mode = label+checkbox, balance-card credit alternatives redeemed on detail page); whatsapp-bar fixed with page padding + scroll-padding, inert when hidden, moved into flow at <480px height/keyboard, inside aside; skip link, per-page <title>, focus h1 after route change, landmarks; form errors (focus first invalid or error summary role=alert, aria-invalid, aria-describedby, aria-required); unread notifications as sr-only text, tab badge in link name; bottom-tab-bar as nav+aria-current; aria-disabled with reason instead of disabled; Toast usage rule.
- (change) Review fixes (a11y M9): accessibility statement is a mandatory legal page — required fields (compliance level, what was made accessible, known limitations, accessibility contact name/phone/email), auto last-updated date on publish, cannot be hidden after first publish; before first publish the page shows only a safety-net line with existing contact details (no invented content) and admin home 'לטיפול' flags it; linked from public footer, menu-sheet, login, customer profile bottom, admin 'עוד'/side-nav.
- (change) Review fixes (rubric majors, customer side): public top-bar gets menu button (lucide Menu, inline-end) + new component menu-sheet; password-reset link surfaces (valid/used/expired, CAP-7); business-side cancellation 'pending choice' state + choice sheet for single/couple, punch card returns without choice, deadline left as open question; leave-waitlist action (waitlist is a separate record, not a booking state); status-chip variant 'expiring'.
- (change) Review fixes (rubric majors, admin side): new Key Flows 6 import + activation links (CAP-31), 7 content/gallery draft-preview-publish with per-image consent+alt (CAP-27/29), 8 customers search/reset link/CSV export/remove personal details (CAP-7/25/30), 9 broadcast (idempotent) + products + audit log + settings (CAP-32/3/26); new 'מצבי אדמין' table (content, session edit with registrants → impact preview, manual registration when full requires explicit capacity raise first, products, broadcast compose/preview/sent, import, export, anonymized profile); sensitive-confirm table adds credit alternatives/extension and import approval. Corrects the earlier 'IA closure check' event — admin journeys were missing until now.
- (change) Review fixes (minors): Flow 2 climax 'הקישור מוכן לשליחה', repeat-purchase branch (CAP-6), couple-booking primitive, offline microcopy no longer implies queued actions, offline disables all data-changing buttons incl. admin, home empty state, dead notification target, couple offset cancel → Tal handles, cancel-window as {חלון ביטול} setting, hero CTA navigates, refund requests only under 'לטיפול', products-catalog microcopy referenced not copied, Ziona FAB note, typography-1 linked as rejected, glossary.md in sources, attendee-row dietary prefs, tab badge colors, <time> dates, admin desktop table a11y. Open Questions 6-11 added (business-cancel choice deadline, couple guest details, logged-in customer on public pages, cancel-window change scope, display-lg weight, [ASSUMPTION] list for Finalize).
- (decision) Couple booking: sheet has one optional field for the second adult's dietary restrictions/allergies — no name
- (decision) Business-side cancellation: no deadline for customer to choose credit vs refund; Tal sees who hasn't chosen (in 'לטיפול')
- (decision) Logged-in customer on public session page sees 'להרשמה' (leads to booking in customer area) and no WhatsApp join bar
- (decision) Changing the self-cancel window in settings applies to new bookings only; existing bookings keep the terms shown at confirmation
- (decision) Large headings (display-lg etc.) use Heebo 300; weight 200 kept only for the big wordmark on plain background
- (decision) 'מלא' status chip stays muted (expired tone), not red
- (decision) Friday special (couple) sessions accept couple bookings only — each booking takes 2 places
- (decision) Adopt mockup interim solutions as spine decisions: customer-area header = simple wordmark row + h1 'היי {שם}'; booking sheet title '{n} התאריכים שבחרת', confirm '{n} המקומות שלי — לשמור'/'לשמור לי את {n} המקומות'; per-date result chips saved=confirmed, not-saved=expired tone with warning inline-notice reason; expiring punch card shows 'עוד X ימים' without CTA; admin 'לטיפול' rows reuse notification-item look (single target + status chip); morning-view summary as card with numeral-lg; new-customer payment fields name+phone; payment methods ביט/פייבוקס/העברה בנקאית/מזומן; new/existing choice as radio cards; busy button not dimmed (spinner, same width); admin home sum period = current month to date; WhatsApp send link as button-primary with icon
- (decision) Mock coverage: key screens mocked = public home, public session, customer home, booking sheet flow, admin home + morning view, admin payment approval. All other surfaces (onboarding form, cancel/credit sheet, content editor, import, etc.) are spine-only by user's selection
- (change) Finalize: lifted memlog 46-55 into spines (Heebo 300 headings + wordmark-display 200 token, muted 'מלא', couple-only Friday special, mockup interim solutions incl. new components task-row/summary-card/radio-card, busy state, session-page order, 'לכל הבראנצ׳ים' link, cancel deadline shown as computed incl. Shabbat); promoted 9 artifacts to mockups/ with inline links; polished structure (EXPERIENCE sections reordered, invented sections nested), removed DESIGN/EXPERIENCE duplication, trimmed Open Questions to 4
- (event) spines finalized
- (override) USER CORRECTION: payment approval for a NEW customer — Tal enters NO customer details. She picks product (amount auto-filled from price), purchase date defaults to today, payment method; reference and note stay optional. Result: one-time join link + WhatsApp button. Customer fills all personal details in the join form (CAP-4) and the system creates the customer from it. Supersedes memlog entry 49 'new-customer payment fields name+phone'. Existing customer: Tal picks the customer (CAP-6).
- (override) USER CORRECTION: every 'reason' field across the platform is OPTIONAL (price change, balance correction, extension, credit alternatives, couple offset, refunds, cancellations, etc.). Sensitive-confirm checkbox remains required. Conflicts with SPEC wording ('נשמר עם סיבה', CAP-2/11/26) — needs SPEC back-propagation.
- (change) Applied user overrides: new-customer payment has no customer fields (product/amount/date-default-today/method; reference+note in optional 'פרטים נוספים'; link+WhatsApp; customer created from join form), existing = search customer; all reason fields optional platform-wide, sensitive-confirm checkbox stays the only requirement; EXPERIENCE IA/Flow 2/radio-card/sensitive-confirm/microcopy/Open Q5 (SPEC back-prop) updated; key-admin-payment mockup (+.working) updated; DESIGN unchanged
- (override) USER DIRECTIVE: source document brunch_at_tal_charecter.md is the source of truth — wins over SPEC.md/companions and these spines on any conflict, except decisions made jointly with the user in this memlog. Spines to be audited against it.
- (change) Source audit (brunch_at_tal_charecter.md wins over SPEC): EXPERIENCE join flow aligned to source §4 — form fields (full name in Hebrew, phone, email as username, baby name + birth date, allergies & dietary as free text, password + confirm, required consent; product/amount locked), join success shows purchase confirmation + balance + expiry + per-product message/button, new states 'entitlement already expired at join' (no auto-extension, contact Tal) and 'existing account' (login first, attach only after login). New-customer confirm button = 'אישור תשלום ויצירת קישור'; 'העתקת הקישור' added; link statuses ממתין למימוש/מומש/פג תוקף/בוטל + redeemed-by + cancel/replace; existing customer found by name or phone, then 'לרישום לתאריך' (source §2).
- (change) Source audit, admin + customer: sensitive-confirm list adds 'שליחת הודעה כללית' and question-form title ('האם לאשר החזר?'); customers tab gets search by name/phone, last-date + active/inactive filters (never-participated counts from account creation), CSV full/partial, phone change via Tal only; profile: customer edits only name, babies, allergies/dietary, phone+email read-only; session fields incl. end time; settings incl. business address/directions/navigation link; contact page incl. directions; about incl. fit for maternity leave; guests cannot join waitlist; waitlist 'place freed' copy says matching entitlement needed and place guaranteed only after booking; couple = 'שני מבוגרים', companion field renamed 'המלווה'; babies not counted in capacity; gallery lazy-load + mobile crop.
- (change) Source audit, mockups (mockups/ + .working/ synced): key-admin-payment button 'אישור תשלום ויצירת קישור', dialog title 'האם לאשר שינוי מחיר?', chip 'ממתין למימוש', added 'העתקת הקישור'; key-admin-home pending/expired join links show 'לקוחה חדשה' instead of invented names (per memlog 53), companion label 'המלווה'; key-customer-home adds purchase-confirmation line; key-public-home Friday title neutral, contact line labelled WhatsApp; 'מגיעות' wording in all mockups. Open Questions: §12 defaults listed as unconfirmed with Tal, new list of source ambiguities, old 'update SPEC' question replaced by a single SPEC drift note. Spines cite source (§n) as authority.
- (decision) Allergies & dietary preferences: ONE free-text field only (incl. vegetarian/vegan/gluten-free/dislikes), no 'no known allergies' checkbox; empty = 'טרם נמסר'; admin sees text as written
- (decision) Continue-to-booking: existing customer — Tal, right after payment approval; new customer — the customer herself at the end of the join form (product button leads to session selection)
- (decision) Customers 'last date' filter = last activity of any kind (participation, purchase or booking — the latest)
- (change) Sync with re-derived SPEC (2026-09-23): join form field 'שם מלא' without Hebrew requirement (form + Flow 3; no 'בעברית' in key mockups); money shown in shekels only (DESIGN price rule + microcopy); export = admin-only bottom-sheet with scope (all / current filtered list) plus column checklist; email change admin-only: profile shows email read-only with 'לשינוי המייל — פנייה לטל', admin customer-profile state and Flow 8 branch mirror phone change.
- (change) Open Questions: section 12 defaults marked resolved (couple 4 weeks, end-of-day expiry, credit replacement per section 6, temporary-data item dropped); ambiguities (d)(f)(h) plus money marked resolved, only (g) imported-customer claim fields remains; SPEC drift note removed, replaced by one line that SPEC was re-derived 2026-09-23 and is aligned.
- (decision) User emphasis (per source §2/§7): every business value — prices, validity/expiry, redemption days, entries, cancel window, registration close, capacity, notification texts, business details — is editable in the admin panel BOTH globally (catalog/settings defaults) AND per case (a specific payment, entitlement, customer or session), via explicit admin actions with confirm+audit; nothing hard-coded
- (decision) User emphasis (per source §3/§7): the entire public marketing site is editable in admin — all texts, headings, images, gallery, testimonials, FAQ, about, contact, payment/arrival instructions, privacy and accessibility pages — with draft/preview/publish, ordering and hiding; no hard-coded marketing copy or images
- (change) Principle 1 (business values editable globally + per case): EXPERIENCE admin IA now says where each value is edited - defaults in Products (all fields incl. party size, intro-only, post-join message) and Settings (CAP-34: validity, close rule, capacity, cancel window, expiring threshold, inactivity period, notification templates); per case in payment amount, entitlement detail (validity/balance/redemption days/session type corrections via sensitive-confirm), customer profile (prior-participation correction), session edit (capacity, close, optional display price, editable notice text). Matrix linked to SPEC companion admin-configurable-parameters.md, not duplicated. New states (settings, templates, entitlement correction), sensitive-confirm rows, microcopy, Flow 9 per-case branch. New component value-change-row (current->new, no checkbox).
- (change) Principle 2 (whole public site editable): public IA blocks marked [תוכן] / [תוכן: פרטי העסק] / [מפגש] (hero photo, headline, description, CTA label, section headings, short explanation, how-it-works, about, FAQ, gallery, testimonials, contact, footer texts, privacy); WhatsApp number and prepared message from business details (seed 0544256456). Content editor structure, empty-block and business-details states, Flow 7 branches (hero, how-it-works/FAQ/about/footer, business details). New components content-section-row (accessible reorder/hide) and image-upload-field (alt text, mobile crop, consent) in EXPERIENCE and DESIGN. Status stays final; all {token} refs resolve.
- (decision) User: move to admin settings (global default, applies to new items only): number of credit alternatives (default 2) and reminder lead time (default 24h before session). Join/claim link validity stays fixed at 48h (security).
- (decision) User confirmed: post-join/purchase message and button text are editable per product; source §4 texts are the defaults.
- (decision) User confirmed: entitlement term correction with future bookings that no longer fit — bookings stay, Tal sees a warning listing them before confirming and handles them manually; no auto-cancel.
- (decision) User confirmed: capacity, registration close, display price and general settings changes use old→new confirmation only, no checkbox; checkbox stays for the source's sensitive list and entitlement corrections.
- (change) Applied 4 decisions to EXPERIENCE: settings now list credit-alternatives count (new credits only) and reminder lead time (new bookings only), link validity 48h fixed and not shown; credit microcopy and flows use the credit's stored count (default two), push-explainer drops 'יום לפני'; post-join message/button shown as the product's current fields (source 4 = default), Open Questions ref fixed to 6(א); entitlement-correction state rewritten, [ASSUMPTION] removed: inline-notice warning inside sensitive-confirm-dialog lists no-longer-fitting future bookings, they stay for manual handling; capacity/close/display price/general settings stated as value-change-row only, no checkbox. DESIGN unchanged. Status stays final; all {token} refs resolve.
